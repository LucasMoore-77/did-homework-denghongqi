# R6 极端值处理

## 检验目的
判断极端生产率是否驱动结果。

## Prompt（执行前保存的任务指令）
【目标】读取 data/raw/digital_transformation_firm_panel.csv。在内存对log_tfp按全样本1%/99%分位缩尾，保持样本与其他设定。

【边界】仅为合成数据教学演示，不解释为真实企业因果证据；data/只读，变换在内存完成；不虚构文献，不选择性隐藏结果；发现不能识别或必要输入缺失先停止并说明。

【验证】报告阈值、被替换数量、系数变化率，缩尾改变结果变量故估计量也会略变。 每项同时核验个体×时间唯一、样本构成、处理变化和原数据SHA256不变。

【汇报】报告估计值、聚类标准误、参考自由度、p值、样本量、企业数及本项特定诊断；输出CSV与必要图形，说明符合/不符合预期及下一步。统计显著性不能取代识别论证。

## 结果摘要与观察
运行后据实际输出填入，保留不符合预期的证据。
