# AI 使用与审查记录

工具使用范围：Codex负责整理任务指令、编写并执行分析程序、辅助解读结果和起草文本。以下记录所用输入、接受或修订的输出及验证依据；经济解释与署名内容仍需本人核对。

## 初始化
从课堂 demo-project 选择性复制数据、DGP、Python/Stata脚本及基线；不复制其他学生代码、Git 历史与结论。原论文留存 source/paper/main.tex；本次不使用 Matlab。参考三份同学材料的组织、证据索引与反思深度，具体统计解释均独立复核。

## Part A 运行中发现与修复
原课堂Python公式同时加入企业FE和时间不变的export_share、soe，在当前statsmodels环境下产生0.12083678、SE=5.82118的异常结果。已保留原CSV与日志到output/iterations；不接受这一输出。移除被FE吸收的两项，用满秩虚拟变量OLS与PanelOLS交叉验证。T4同时加入两套类别年虚拟变量时触发秩检查，改为显式省略各类别及年份基准；未关闭秩检查。

## Part B / 第一轮迭代：由尾部比例到有限模拟证据
工具：Codex执行本地Skill指令及Python脚本，没有调用独立外部模型。原型Skill见4e57924。裸Prompt与原型均为300次、同一种子、同一企业分配；实际结果tail_count=0、tail_rate=0。该数字本身是正确计数，但没有有限模拟分辨率，易被误读为p=0。修订Skill与辅助程序后重新运行：平滑比例=0.003322，分辨率相同；单侧95%上界=0.009936。二者不是新的现实因果证据。
对照设计控制了数据、种子、抽样和模型，只改变验证与汇报制度；两次300个系数的最大绝对差=0，结果没有被调到更显著。Skill还保留哈希与变量映射，吸收两个不变控制项。没有任何失败重复。审查拒绝“随机化p=0”“安慰剂排除内生性”的说法。
证据：output/iterations/skill_v1/summary.json、output/skill_r2/summary.json、两份draws.csv。

## Part C 初版评估
Codex按S1—S6进行主题解释，辅助脚本生成报告；没有独立模型调用。报告接受R5精度警示、R7估计对象变化、T3未获支持。检查目前仅验证主CSV结构和重复行，尚未提供缺失文件或篡改数字的负向测试证据；这一限制将在第二轮迭代检查。Agent配置只有Read/Glob/Grep，报告由调用者保存，未给Agent论文写权限。

## Part D 论文
以中文LaTeX重写论文，删除本次不涉及的理论校准，使用T3实证讨论。新增完整稳健性正文与全部回归附表；核查3篇方法文献并保存Crossref元数据。使用Tectonic 0.15.0（XeTeX引擎）实际编译，保留日志。正文区分同步ATT目标、短窗口动态平均、强度代理、少簇精度与不显著的机制。评估报告没有直接写论文，由本任务单独执行。

## Part E / 第二轮迭代与迁移
实际错误输入测试显示初版2/6正确，修订后6/6正确，见output/iterations/agent_v1_tests.json与agent_v2_tests.json。临时副本测试未污染原数据；评估只返回报告，受保护文件哈希保持不变。最低工资合成面板更换研究主题、列名、时间与效应方向，原Skill程序完成120次模拟，并拒绝重复键、反转和交错采用。独立满秩OLS与CGM协方差用于交叉验证二维聚类，结果见verification.json。

## 来源、工具与接受边界
课程数据、DGP、参考Stata来自demo-project。参考陈圣恩的证据组织、刁绍华的聚类反思、饶如意的迁移讨论；没有复制其代码、数字、Git历史或个人经历。对参考材料中“p大即通过”“取最大SE即可解决少簇”等简化结论未照搬。指定submit-paper技能仅借鉴核查思路，未开展期刊投稿评估。

使用skill-creator指导技能设计；使用documents、pdf完成材料及渲染。Skill结构校验在安装PyYAML后通过；此前缺少依赖的验证失败未当作成功。依赖安装时外置盘生成的AppleDouble元文件干扰matplotlib，清理本次新建虚拟环境内元文件后恢复。本次借助Codex审查关键公式、固定效应交互构造、二项尾部解释和Agent输出权限。

## 最终复现与视觉检查
完整reproduce.py已成功执行。论文5页经LaTeX实际编译与逐页PNG检查；纸质材料7页完成Word、PDF与Markdown。LibreOffice初始字体范围不包含中文字体，先拒绝缺字PDF，补充Noto CJK与当前进程fontconfig目录后重渲染，并调整固定行距与分页，消除溢出和空页。最终文件包含清楚的姓名学号、两张完整Prompt、两轮实际对照、Git文本及清单。仅真实GitHub链接和远程推送由本人补充。
